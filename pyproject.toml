[project]
name = "bearcli"
version = "1.2.1"
description = "The missing CLI for Bear notes — read, search, export, and manage your notes from the terminal"
readme = "README.md"
keywords = ["bear", "notes", "cli", "markdown", "macos"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Environment :: Console",
    "Operating System :: MacOS",
    "Programming Language :: Python :: 3.13",
    "Topic :: Utilities",
]
authors = [
    { name = "Michel Tricot", email = "michel.tricot@gmail.com" }
]
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.13"
dependencies = [
    "detect-secrets>=1.5.0",
    "rapidfuzz>=3.14.5",
    "textual>=8.2.8",
    "typer>=0.27.0",
]

[project.urls]
Homepage = "https://michel-tricot.github.io/bearcli/"
Repository = "https://github.com/michel-tricot/bearcli"
Documentation = "https://github.com/michel-tricot/bearcli/blob/main/docs/IMPLEMENTATION.md"

[project.scripts]
bearcli = "bearcli.cli:app"

[build-system]
requires = ["uv_build>=0.8.22,<0.9.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.0",
    "ty>=0.0.64",
]

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]
